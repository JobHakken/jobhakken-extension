{
  "name": "first2apply-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "node build.mjs",
    "test": "jest --config ./jest.config.ts"
  },
  "dependencies": {
    "@first2apply/autofill": "workspace:*"
  }
}
