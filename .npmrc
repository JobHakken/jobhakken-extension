# @jobhakken/* shared libraries are published to GitHub Packages (private).
# Auth: set NODE_AUTH_TOKEN to a GitHub token with read:packages (CI uses GITHUB_TOKEN).
@jobhakken:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
