// Mirrors the monorepo base style (printWidth 120, single quotes). No import-sort /
// tailwind plugins — this repo has neither. .cjs because package.json is "type":"module".
module.exports = {
  printWidth: 120,
  singleQuote: true,
};
